PROMPT_STAGE_1_VISION = r"""
任务：请对这张 PPT 图片进行详尽的视觉提取。

【思考要求】
1. **Figure 价值判断**：首先扫视全图，判断是否存在**具有信息量的 Figure**（如架构图、流程图、物理模型图、函数曲线、分子结构等）。
   - **忽略**：页码、纯装饰性背景线条、无意义的库存图片、简单的排版框。
   - **关注**：任何承载知识逻辑的图示。
2. **TikZ 预备分析**：对于有价值的 Figure，请在脑海中解构其几何拓扑（节点形状、位置关系、箭头流向）。

【提取要求】
1. **OCR 识别**：逐字提取文本，保留原始换行结构。
2. **公式识别**：所有数学/物理公式转换为 LaTeX 格式。
3. **[重要] Figure 深度描述**：
   - 如果发现有价值的 Figure，请单独输出一个 `### Figure Analysis` 区块。
   - **描述粒度**：请用自然语言详细描述，以便我稍后能根据你的描述画出 TikZ 代码。
   - **包含要素**：
     * **布局**：例如“左侧是A，右侧是B”，“呈环形分布”，“分层结构”。
     * **节点**：形状（圆/矩形/菱形）、颜色、包含的文字。
     * **连接**：箭头方向（A指向B）、线型（实线/虚线）、连线上的标签。
   - 如果图片仅为纯文本或无意义装饰，则**不要**输出此区块。

请直接输出提取到的内容，保持原意，**不需要**整理成 Markdown 格式。
"""


PROMPT_STAGE_2_BRAIN = r"""
你是一个严格的单页 Markdown 转写器。你的任务是只把 [Target] 当前页 Raw Data 整理为 Markdown。

最重要的边界：
- 只能输出 [Target] 当前页中实际出现的内容。
- [P-2]、[P-1]、[N+1]、[N+2] 只能作为后台参考，用于判断 OCR 错字、符号一致性、术语一致性。
- 严禁把前页或后页的正文、标题、例子、结论、下一节内容写进当前页。
- 如果 [Target] 页末尾是截断句子、半句话、残缺公式，不要用 [N+1] 补全；保持当前页到此为止。
- 如果 [Target] 页开头承接上一页，不要把 [P-1] 的前半句补进来；只整理当前页看得到的部分。
- 不要输出任何解释、寒暄、声明、处理过程或“我已经分析了...”之类的话。
- 最终答案必须从 `# Slide {slide_no}` 开始，且只能包含当前页 Markdown 内容。

【五页滑动窗口信息 (全 Raw Data)】
--------------------------------------------------
[P-2] 前前页 (Raw OCR Data):
{prev_2_raw}
--------------------------------------------------
[P-1] 前一页 (Raw OCR Data):
{prev_1_raw}
--------------------------------------------------
[Target] **当前页 (Raw OCR Data - 包含详细的 Figure 描述)**:
{target_raw}
--------------------------------------------------
[N+1] 后一页 (Raw OCR Data):
{next_1_raw}
--------------------------------------------------
[N+2] 后两页 (Raw OCR Data):
{next_2_raw}
--------------------------------------------------

【任务流程】
请在后台完成判断，不要输出思考过程：
1. **单页边界判断**：
   - 以 [Target] 为唯一内容来源。
   - 前后页只能帮助判断“当前页里的字/符号是否识别错了”，不能提供新增正文。
   - 不得为了让语义完整而跨页补写。
2. **符号一致性 (Crucial)**：
   - 可以对比前后页 Raw Data 修正 [Target] 内部明显 OCR 错误，例如 $v$ / $\nu$、上下标、常见术语。
   - 修正必须服务于 [Target] 中已经存在的内容；不得因此引入前后页的新句子。
   - 必须使用 LaTeX 格式：行内公式 $...$，行间公式 $$...$$。不要使用 `\[` 和 `\]`。
3. **Figure 整合**：检查 [Target] 中是否有 `### Figure Analysis` 区块。
   - 如果有，请在 Markdown 中将其整理为 `> [!NOTE] Figure 描述` 引用块。
   - Figure 描述也只能来自 [Target] 的 Figure Analysis，不要根据前后页扩展图意。

【输出要求】
只输出当前页的 Markdown。不要输出“好的”“以下是”“作为编辑”“根据五页 Raw Data”等任何开场白或结尾说明。

结构如下：

1. **正文**：
   - 标题：# Slide {slide_no}
   - 内容：只包含 [Target] 当前页可见内容整理后的文本、列表、表格和 LaTeX 公式。
   - 如果当前页原文没有标题，不要从前后页借标题。
   - 如果当前页原文在页尾截断，输出也在相同位置停止。
   - **图像描述**（如果有）：
     > [!NOTE] Figure 描述
     > (在此处放入整理后的图像几何描述，方便后续 TikZ 绘图)

2. **原文勘误** (可选)：
   - 只有在 [Target] 内部存在明显 OCR 错误，且需要说明时才输出。
   - 勘误只解释当前页内部修正，不要写“根据 P-1/N+1 补全了...”。
   > [!WARNING] 原文勘误
   > ...

禁止输出：
- 任何 `<CTX>` 元数据。
- 任何前后页 Raw Data 的正文。
- 任何“符号统一说明”“处理依据”类型的编辑日志，除非它是当前页 OCR 勘误且非常必要。
- 任何 Markdown 代码围栏包裹全文。
"""
