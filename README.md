# PPT2MD-Qwen-VL (V6.0) 🚀

> 基于 **Qwen3-VL-Plus** (通义千问视觉大模型) 的智能 PPT 转 Markdown 工具。
> 支持 **公式识别 (LaTeX)**、**图表深度理解**、**上下文连贯** 以及 **多任务并发**。

## 📖 简介

本项目旨在将大量的课件/论文/会议 PPT 图片批量转换为结构清晰的 Markdown 笔记。

不同于普通的 OCR 工具，本项目利用了大模型的**视觉推理能力**：

1. **精准公式**：自动识别行内/行间公式并转换为标准 LaTeX 格式 (`$$...$$`)。
2. **上下文连贯**：模型“记得”上一页讲了什么，避免重复输出，同时保证知识点的连续性（通过 `<CTX>` 隐藏状态块实现）。
3. **视觉描述**：自动分析架构图、流程图、数据图表，并生成详细的文字描述。
4. **智能断点续传**：任务中断后，下次运行自动检测进度，无需重跑已完成的页面。
5. **优雅交互体验**：基于 `rich` 库构建的**多行并行进度条**，实时监控每个 PPT 的处理速度和剩余时间。

---

## ✨ 核心特性 (V6.0)

* **💻 沉浸式终端 UI**：告别丑陋的滚动日志！使用固定进度条显示多任务状态，包含百分比、速度 (s/页)、预计剩余时间 (ETA)。
* **📜 完善的日志系统**：所有调试信息、报错详情、API 状态码均自动归档至 `./log/log_YYYYMMDD_HHMMSS.log`，不再干扰屏幕显示。
* **📂 自动分组与隔离**：自动扫描 `ppt_images` 下的子文件夹，输出结果自动隔离到 `markdown_output` 对应目录。
* **⚡ 智能并发控制**：支持同时处理多个 PPT（默认并发数 3），内置 QPS/TPM 保护机制。
* **🧠 深度思考模式**：启用 `Thinking Mode`，极大提升对复杂图表的理解能力（UI 上只显示状态，不刷屏）。
* **🎮 交互式控制**：启动时自动扫描任务，支持选择特定 PPT、指定页码范围（如 `1-10`, `5-end`）。
* **🔄 断点续传**：自动跳过已生成的文件，并读取前一页的上下文状态，无缝接续生成。
* **📑 自动合并**：单页生成完毕后，自动汇总为 `XXX_FULL.md`。

---

## 🛠️ 环境准备

### 1. Python 环境

需要 Python 3.8 或更高版本。

### 2. 安装依赖

本项目依赖阿里云官方 SDK 和 UI 库 `rich`。

```bash
pip install -U dashscope rich

```

### 3. 配置 API Key

你需要开通阿里云百炼服务并获取 `Qwen3-VL-Plus` 的权限。

*方式 A (推荐 - 环境变量)*:

```powershell
# Windows PowerShell
$env:DASHSCOPE_API_KEY="sk-你的密钥"

# Linux / macOS
export DASHSCOPE_API_KEY="sk-你的密钥"

```

*方式 B (修改代码)*:
打开 `ppt2md.py`，修改顶部配置区：

```python
# dashscope.api_key = "sk-你的密钥"  <-- 去掉注释填入

```

---

## 📁 目录结构说明

请按照以下结构组织文件（**无需手动创建 output/log 文件夹**，脚本会自动创建）：

```text
ProjectRoot/
├── ppt2md.py                # 主程序脚本
├── session_state.json       # (自动生成) 任务进度存档
├── log/                     # (自动生成) 日志归档目录
│   └── log_20231220_143000.log
├── ppt_images/              # 【输入目录】在此目录下建立子文件夹
│   ├── 课件_原子物理01/      # 任务 A
│   │   ├── slide_01.png
│   │   ├── slide_02.png
│   │   └── ...
│   ├── 课件_原子物理02/      # 任务 B
│   │   ├── img_001.jpg
│   │   └── ...
│   └── 总复习PPT/            # 任务 C
│       └── ...
└── markdown_output/         # 【输出目录】(脚本自动生成)
    ├── 课件_原子物理01/
    │   ├── Slide_01.md      # 单页结果
    │   ├── ...
    │   └── 课件_原子物理01_FULL.md  # 最终汇总文件
    └── ...

```

---

## 🚀 使用指南

### 1. 启动程序

在终端运行：

```bash
python ppt2md.py

```

### 2. 交互式选择

程序启动后会扫描 `ppt_images` 下的所有子文件夹，并进入交互模式：

```text
🔍 正在扫描 PPT 文件夹...

📂 发现以下 PPT:
  [1] 课件_原子物理01 (80 页)
  [2] 课件_原子物理02 (20 页)

👉 请选择要处理的 PPT:
   输入序号(如 1,3) | 输入 'a' 全选
   您的选择: 

```

### 3. 配置页码范围

选择 PPT 后，你可以指定处理范围（支持断点续传逻辑）：

* `all` 或 `回车`：处理所有页面。
* `1-10`：只处理第 1 到第 10 页。
* `20-end`：从第 20 页处理到最后。

### 4. 运行效果

程序将启动多进程池，屏幕上会显示优雅的进度条：

```text
课件_原子物理01 ━━━━━━━━━━━━━━━━━━━━━━━━━╺ 85% • 68/80 • 0:12:30 • -0:02:10 P68 生成中...
课件_原子物理02 ━━━━━━━━━━━━━━━━╺━━━━━━━ 60% • 12/20 • 0:05:10 • -0:03:20 P12 深度思考...
总复习PPT      ━━━━━━━━━━━━━━━━━━━━━━━━━━ 100% • 15/15 • 0:03:00 • 0:00:00 全部完成

```

---

## ⚙️ 性能调优与限流分析 (Technical Detail)

本项目默认参数经过严格的数学推导，旨在平衡**生成速度**与**API 限流风险**。

### 1. 默认配置

```python
MODEL_NAME = "qwen3-vl-plus"
MAX_WORKERS = 3       # 并发数
THINKING_BUDGET = 2048 # 思考Token限制

```

### 2. 为什么 `MAX_WORKERS` 推荐设为 3？

基于阿里云官方 Qwen3-VL 的限流规则：

* **TPM (Tokens Per Minute)**: 1,000,000
* **TPS (Tokens Per Second)**:  ()

#### Token 计算公式

官方图像 Token 计算公式：


假设处理标准 1080P PPT 截图 ()：



加上 Prompt 和上下文文本 ( Token)，单次请求总消耗约 **3,027 Token**。

#### 并发压力推导

* **若并发 = 3**：瞬间峰值  Token。
*  (TPS 阈值)，**安全 (占用率 54%)**。


* **若并发 = 5**：瞬间峰值  Token。
* 接近阈值，如果遇到几张 4K 大图 ( Token)，瞬间流量将达到 ，**必触发 429 限流**。



**结论**：保持 `MAX_WORKERS = 3` 是最稳妥的策略，既能跑满带宽，又留有处理 4K 大图的安全余量。

---

## ❓ 常见问题 (FAQ)

**Q: 为什么日志里看不到“思考过程”了？**
A: 为了保持界面整洁，思考过程被隐藏了，只在 UI 状态栏显示“深度思考...”。如果你非常需要看模型在想什么，请去 `./log/` 文件夹查看详细日志。

**Q: 任务中断了怎么办？**
A: 直接重新运行脚本。程序会检测到 `session_state.json`，询问是否继续。选择继续后，它会自动跳过已生成的 `md` 文件，并自动读取断点处的上下文，无缝继续。

**Q: 为什么运行到一半报错 `list index out of range` 或 `KeyError`?**
A: V5.0+ 版本已修复此问题。这是由于流式传输中的空包导致的，新代码增加了 `safe_get_content` 保护机制。

**Q: 输出的 Markdown 最后有 `<CTX>` 标签，我不想要怎么办？**
A: 在代码 `merge_markdowns` 函数中，取消 `re.sub` 行的注释即可在合并时自动过滤掉这些元数据。

**Q: 为什么不能所有图片同时并发？**
A: 为了保证 PPT 的**上下文连贯性**（比如第 2 页引用第 1 页的定理），同一个 PPT 内必须按顺序（串行）处理。但不同的 PPT 之间是完全并发的。

---

**Disclaimer**: 本工具基于阿里云 DashScope API 开发，使用过程中会产生 Token 费用，请关注您的阿里云账户余额。